/** Public URLs for nav, feedback, and one-click demo (no secrets). */

export const SITE_REPO = 'https://github.com/herbyxo/Orbit'

export const FEEDBACK_NEW_ISSUE = `${SITE_REPO}/issues/new`

/** Default “try it” repo — must be public on GitHub for /api/parse-repo */
export const DEMO_REPO_URL = SITE_REPO
